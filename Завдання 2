# Завдання 2, варіант 18
# Знайти суму і кількість елементів ряду y = 1 * 3 * 5 * ... * (2n - 1), при n = 20

n = 20  # задане за умовою

# створюємо ряд непарних чисел
numbers = [2 * i - 1 for i in range(1, n + 1)]

# знаходимо суму
sum_y = sum(numbers)

# кількість елементів
count_y = len(numbers)

# виводимо результати
print("Елементи ряду:", numbers)
print("Кількість елементів:", count_y)
print("Сума елементів:", sum_y)
