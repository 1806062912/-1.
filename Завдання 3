# Завдання 3, варіант 18

N = int(input("Введіть ціле число N (1 < N < 9): "))

if N <= 1 or N >= 9:
    print("Помилка: N має бути в межах 1 < N < 9.")
else:
    # Верхня частина
    for i in range(N, 0, -1):
        for j in range(i, N + 1):
            print(j, end=" ")
        print()

    # Нижня частина
    for i in range(1, N + 1):
        for j in range(1, i + 1):
            print(j, end=" ")
        print()
